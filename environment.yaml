name: benchmark
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - torch>=2.1.0
    - torchvision>=0.16.0
    - ultralytics>=8.0.0
    - opencv-python-headless>=4.8.0
    - numpy>=1.24
    - pyyaml>=6.0
    - psutil>=5.9
    # Optional: TensorRT export
    # - tensorrt>=8.6
    # - onnx>=1.14
    # - onnxruntime-gpu>=1.16
